interface:
  display_name: "Dev PR"
  short_description: "Open PRs; do not arm or apply Forge"
  default_prompt: "Use $dev-pr as 开发端: open a PR titled type(product/actor): subject (must pass python -m forge pr-title), fix armed-red, request review. Do not invent branch prefixes. Do not push protect, self-merge, arm Overlay, or live forge apply."
