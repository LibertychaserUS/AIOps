# Optional companion to suites/<id>/cases.md.
# Traces an adopter-minted function_id to a case. Kernel example — not a catalog.

schema: overlay-trace/v1
suite: checkout-retry
items:
  - function_id: FN-login-retry
    case_id: FN-login-retry/functional
    type: functional
    level: unit
  - function_id: FN-login-retry
    case_id: FN-login-retry/functional
    type: functional
    level: e2e
  - function_id: FN-checkout-idempotent
    case_id: FN-checkout-idempotent/negative
    type: negative
