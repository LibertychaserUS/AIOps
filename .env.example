# Overlay generate — model API only. Not GitHub write. Token only on explicit generate.
# OPENAI_API_KEY=

# Dev/agent 代推 (forge submit). Required. Not Ops apply. Not CI GITHUB_TOKEN.
# FORGE_SUBMIT_TOKEN=

# Ops apply (Administration: write). Not submit.
# FORGE_GITHUB_TOKEN=
