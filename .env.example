# Overlay generate — model API only. Not GitHub write. Token only on explicit generate.
# OPENAI_API_KEY=

# Ops apply (Administration: write). Not submit. Not host-injected 代推.
# FORGE_GITHUB_TOKEN=
