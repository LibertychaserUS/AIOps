# Dev / agent 代推 (`python -m forge submit`, including --dry-run).
# PAT / fine-grained token / GitHub App installation token.
# Not CI GITHUB_TOKEN. Not Ops apply. Never commit the real value. Never print it.
# FORGE_SUBMIT_TOKEN=

# Ops apply (Administration: write). Not the submit key.
# FORGE_GITHUB_TOKEN=
