# 用例设计与标准化覆盖

规范 skill 已改成 Codex / Agent Skills 的 `SKILL.md` 格式，目录名与 `name` 对齐：

[`../../skills/design-cases/SKILL.md`](../../skills/design-cases/SKILL.md)

本文只作旧路径入口。写 `cases.md` / `invariants.yaml`、问全局 corner、跑 `overlay cover` 时读那份 skill。不要把本文当成第二套方法。

操作 Overlay（inbox / validate / select / run）用 [`../../skills/use-overlay/SKILL.md`](../../skills/use-overlay/SKILL.md)。
